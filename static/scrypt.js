var instance = M.Tabs.getInstance(elem);
instance.select('tab_id');

// $(function() {
//     while( $('#summary2 div').height() > $('summary2').height() ) {
//         $('#summary2 div').css('font-size', (parseInt($('#summary2 div').css('font-size')) - 1) + "px" );
//     }
// });